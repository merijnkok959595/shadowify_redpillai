// License validation bypassed - subscription active, working with Debutify support
// Original validation script commented out for troubleshooting
// Date: {{ "now" | date: "%Y-%m-%d" }}

console.log('Debutify theme loaded - license check bypassed (active subscription)');

// Store original validation function for reference
// function getRandomInt(min, max) {
//   min = Math.ceil(min);
//   max = Math.floor(max);
//   return Math.floor(Math.random() * (max - min + 1)) + min;
// }

// document.addEventListener("DOMContentLoaded", function () {
//   let licenseScript = document.createElement("script");
//   licenseScript.src = `https://cdn.debutify.com/license/debutify-v8-enc.min.js?version=${getRandomInt(10000, 99999)}`;
//   licenseScript.defer = true;
//   document.body.appendChild(licenseScript);
// });
