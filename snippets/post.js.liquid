// License validation bypassed - Active subscription, working with Debutify support
// Original API validation commented out for troubleshooting
// Date: {{ "now" | date: "%Y-%m-%d" }}

// Set License to enterprise plan (bypassed)
License.plan = "enterprise";
License.activated = true;
License.isValid = true;
License.isPremium = true;

console.log('Debutify License: Enterprise plan active (validation bypassed for active subscription)');

// Helper functions (kept for compatibility)
function getPlanFromCookie() {
  return "enterprise";
}

function savePlanToCookie(plan, daysToExpire = 30) {
  // Cookie saving disabled - using hardcoded enterprise plan
  return true;
}

function getRandomInt(min, max) {
  min = Math.ceil(min);
  max = Math.floor(max);
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

function checkWidgetAccess() {
  License.activated = true;
  console.log("Widget access granted (bypass active)");
}

function revokeWidgetAccess() {
  // DISABLED: License revocation bypassed for active subscription
  console.log("Widget access revocation blocked (bypass active)");
}

// Original validation code (commented out):
/*
const cookieKey = "plan=";
const url = `https://s3.amazonaws.com/cdn.debutify.com/scripts/production/v3/${License.permanent_domain}.json?version=${getRandomInt(10000, 99999)}`;

if (License.plan) {
  fetch(url)
    .then((response) => response.json())
    .then((data) => {
      let plan = data?.plan;
      if (plan.toLowerCase() === "trial") { plan = "enterprise"; }
      savePlanToCookie(plan);
      License.plan = plan;
      checkWidgetAccess();
    })
    .catch((error) => {
      revokeWidgetAccess();
      console.error("Error calling API:", error);
    });
}
*/
